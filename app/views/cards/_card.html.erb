<article class="card">
  <header>🎯 Ma mission</header>
  <p>Fais en sorte que <strong><%= card.target %></strong> effectue l'action suivante:<br/>
    <strong><%= card.action %></strong>
  </p>
  <%= render 'cards/done_button', card: card %>
</article>
