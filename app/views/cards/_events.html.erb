<% if cards.size != 0 %>
  <ul class="events">
    <% cards.each do |card| %>
      <li>💀<strong><%= format_time card.done_at %></strong>: Un joueur <%= kill_word %>. On l'a fait <strong><%= card.action %></strong>.</li>
    <% end %>
  </ul>
<% else %>
  <p>Tout est calme... pour le moment. 😏</p>
<% end %>
