<% if game.started? %>
  <%= form_with(model: game) do |form| %>
    <%= form.hidden_field :started_at, value: nil %>
    <%= form.submit '⏳ Suspendre la partie', class: :secondary %>
  <% end %>
<% else %>
  <%= form_with(model: game) do |form| %>
    <%= form.hidden_field :started_at, value: Time.now %>
    <%= form.submit '🏁 Démarrer la partie', disabled: !game.can_start? %>
    <% if game.can_start? %>
      <small><%= t 'game_show.start_help' %></small>
    <% else %>
      <small>⚠️ <%= t('game_show.no_enough_players') %></small>
    <% end %>
  <% end %>
<% end %>
